
//Objetivo

Criar um carrinho de compras baseado no carrinho de compras da shopee, aonde o carrinho armazene itens e faça o cálculo de sub-itens automaticamente

modalizar o codigo
organizar o codigo
// dominio da aplicação: carrinho de compras(index)controlador

//as entidades representadas

//=>carrinho = carrinho(cart)service
adiciona item e remove item
calcula preço e quantidade
//=>lista de desejos = lista de desejos(index)
//=>itens = itens(item)service
lista de item a ser lida.
prazo de chegada.
